uvicorn main:app --host 0.0.0.0 --port 8000 --reload


In react component
```
return (
   <div>
        <pre style={{ whiteSpace: 'pre-wrap' }}                 
            {generatedText}
        </pre>
    </div>
```
